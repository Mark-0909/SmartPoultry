<Window x:Class="SmartPoultry.SupplierInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        Title="Supplier Information" Height="570" Width="600" 
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" 
        Background="Transparent" WindowStyle="None" AllowsTransparency="True">

    <Window.Resources>
        <Style x:Key="NoHoverButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation 
                                                Storyboard.TargetProperty="Opacity" 
                                                To="0.7" Duration="0:0:0.2" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation 
                                                Storyboard.TargetProperty="Opacity" 
                                                To="1.0" Duration="0:0:0.3" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Overlay -->
        <Border x:Name="Overlay" Opacity="0.5" CornerRadius="10" BorderBrush="Transparent" Background="#FFA7A7A7" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <!-- Main Container -->
        <Border CornerRadius="10" BorderBrush="#FF077C5E" BorderThickness="2" Background="#FFDDDDDD" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <!-- Header -->
                    <RowDefinition Height="*" />
                    <!-- Content -->
                    <RowDefinition Height="Auto" />
                    <!-- Footer -->
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Background="#FF2C6E5D" CornerRadius="10,10,0,0" Height="40" VerticalAlignment="Top" Margin="-20,-20,-20,0">
                    <Grid>
                        <Label Content="Supplier Information" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" FontWeight="Bold" Foreground="White"/>
                        <Button Click="ExitButton_Click" Background="Transparent" BorderBrush="Transparent" HorizontalAlignment="Right" VerticalAlignment="Center" Width="30" Height="30">
                            <Image Source="/Images/close.png"/>
                        </Button>
                    </Grid>
                </Border>

                <!-- Content -->
                <StackPanel Grid.Row="1" Margin="0,20">
                    <Label Content="Name:" FontWeight="Bold"/>
                    <TextBox x:Name="SupplierName" Height="30" Margin="0,5,0,20" BorderBrush="#CCCCCC" BorderThickness="1" Background="#FFFFFF"/>

                    <Label Content="Contact Person:" FontWeight="Bold"/>
                    <TextBox x:Name="ContactPerson" Height="30" Margin="0,5,0,20" BorderBrush="#CCCCCC" BorderThickness="1" Background="#FFFFFF"/>

                    <Label Content="Phone:" FontWeight="Bold"/>
                    <TextBox x:Name="Phone" Height="30" Margin="0,5,0,20" BorderBrush="#CCCCCC" BorderThickness="1" Background="#FFFFFF"/>

                    <Label Content="Email:" FontWeight="Bold"/>
                    <TextBox x:Name="Email" Height="30" Margin="0,5,0,20" BorderBrush="#CCCCCC" BorderThickness="1" Background="#FFFFFF"/>

                    <Label Content="Address:" FontWeight="Bold"/>
                    <TextBox x:Name="Address" Height="30" Margin="0,5,0,20" BorderBrush="#CCCCCC" BorderThickness="1" Background="#FFFFFF"/>
                </StackPanel>

                <!-- Footer -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
                    <Button Content="Save" Width="100" Height="40" Margin="10,0" Background="#4CAF50" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
                    <Button Content="Delete" Width="100" Height="40" Margin="10,0" Background="#F44336" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
