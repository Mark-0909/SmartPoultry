<UserControl x:Class="SmartPoultry.Supplier_SupplierControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignWidth="600" Width="600" Height="49">
    <Grid Margin="0,5,0,5">
        <!-- Highlight Background -->
        <Border x:Name="HighlightBorder" CornerRadius="10" BorderBrush="Black" BorderThickness="1" 
                VerticalAlignment="Center" Background="White" Height="37">
            <Label x:Name="ContactInfo" Content="CONTACT INFO" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="362,0,0,0" Width="99"/>
        </Border>
        <Label x:Name="Name" Content="NAME" HorizontalAlignment="Left" Margin="32,0,0,0" VerticalAlignment="Center" Width="59"/>
        <Label x:Name="ContactPerson" Content="CONTACT PERSON" HorizontalAlignment="Left" VerticalAlignment="Center" Width="161" Margin="186,0,0,0"/>
        <Button x:Name="EditButton" Content="Edit" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="507,0,0,0" Width="75" Height="25" Click="EditButton_Click"/>

        <!-- Add Trigger Style for Hover Effect -->
        <Grid.Style>
            <Style TargetType="Grid">
                <Setter Property="Background" Value="Transparent" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#FFE0E0E0" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>
    </Grid>
</UserControl>